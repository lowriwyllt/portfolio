import langType from "@/app/constants/langType";

const AdventuresFilter = ({ lang = "en" }: { lang?: langType }) => {
  return <div>Filters coming soon...</div>;
};

export default AdventuresFilter;
